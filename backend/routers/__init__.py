# Prahari Routers Package
# Each router module handles a distinct feature area:
#   vision.py      — POST /scan/process
#   medication.py  — GET  /medication/search, GET /medication/profile
#   triage.py      — POST /triage/assess
#   directory.py   — POST /directory/search
